import * as Lucide from "lucide-react";
console.log(Object.keys(Lucide).filter(k => k.toLowerCase().includes("link") || k.toLowerCase().includes("tube") || k.toLowerCase().includes("face") || k.toLowerCase().includes("twit")));
